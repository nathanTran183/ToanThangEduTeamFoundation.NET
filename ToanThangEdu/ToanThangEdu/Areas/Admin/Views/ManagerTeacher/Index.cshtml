@using ToanThangEdu.Areas.Admin.Models
@model PagedList.IPagedList<ToanThangEdu.Areas.Admin.Models.TeacherCustomizeModel>
@using PagedList.Mvc;
@{
    ViewBag.Title = "QUẢN LÝ GIÁO VIÊN";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";

    //List<TeacherCustomizeModel> listTeacher = new List<TeacherCustomizeModel>();
    //listTeacher = ViewBag.ListTeacher;
}
<br />
<h4 class="text-center">QUẢN LÝ GIÁO VIÊN</h4>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Hình đại diện")
        </th>
        <th>
            @Html.DisplayName("Name")
        </th>
        <th>
            @Html.DisplayName("Full Name")
        </th>
        <th>
            @Html.DisplayName("Email")
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.HomeTown)
            </th>*@
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        //if (item.Id != "1a32ef34-948c-4084-a971-c8e71761719c")
        //{

        <tr>

            @if (item.ImageURL != null)
            {
                <td>
                    <div class="prof-thumb">
                        <img style="width: 40px; height: 40px" src="~/images/Teacher/@item.ImageURL" alt="Profesor Name" />
                    </div>
                </td>
            }
            else
            {
                <td>
                    <label>No Image.</label>
                </td>
            }


            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>

            <td>
                <a class='btn btn-warning btn-sm' href="@Url.Action("Edit", "ManagerTeacher", new { id = item.Id })" role='button'><span title="Chỉnh sửa" class='glyphicon glyphicon-pencil' aria-hidden='true'></span></a>
                <a class='btn btn-danger btn-sm' href="@Url.Action("Delete", "ManagerTeacher", new {id = item.Id })" role='button'><span title="Xóa" class='glyphicon glyphicon-trash' aria-hidden='true'></span></a>

            </td>
        </tr>
            @*}*@
    }

</table>
@(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)/ @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))
