@model PagedList.IPagedList<ToanThangEdu.Models.RegisterTeacher>
@using PagedList.Mvc;

@{
    ViewBag.Title = "QUẢN LÝ DANH SÁCH GIA SƯ";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";

}
<div class="container form-horizontal">
    <div class="row">

        <div class="col-lg-10">
            <div class="widget-item">
                <div class="form-horizontal">
                    <h4 class="text-center">QUẢN LÝ DANH SÁCH GIA SƯ</h4>
                    <hr />
                </div>
            </div>
        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Họ và tên")
        </th>
        <th>
            @Html.DisplayName("Năm sinh")
        </th>
        <th>
            @Html.DisplayName("Số điện thoại")
        </th>
        <th>
            @Html.DisplayName("Email")
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.HomeTown)
            </th>*@
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        //if (item.Id != "1a32ef34-948c-4084-a971-c8e71761719c")
        //{

        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Fullname)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.YearOfBirth)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>

            <td>
                <a class='btn btn-warning btn-sm' href="@Url.Action("Edit", "RegisterTeachers", new { id = item.Id })" role='button'><span title="Chỉnh sửa" class='glyphicon glyphicon-pencil' aria-hidden='true'></span></a>
                <a class='btn btn-danger btn-sm' href="@Url.Action("Delete", "RegisterTeachers", new { id = item.Id })" role='button'><span title="Xóa" class='glyphicon glyphicon-trash' aria-hidden='true'></span></a>

            </td>
        </tr>
        @*}*@
    }

</table>


@(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)/ @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))
